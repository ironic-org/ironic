# Ironic documentation site

The documentation site uses Vite, React Router, Fumadocs, and Tailwind CSS.

```bash
bun run dev
bun run check-types
bun run build
```

Framework documentation lives under `content/docs`. `meta.json` controls sidebar ordering.
